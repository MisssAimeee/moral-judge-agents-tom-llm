# Pending rescore — NOT talk-safe

Figures here contain closed-model rows scored 2026-07-21 against stimulus master **v1**
(contaminated: story-boundary leakage, CV leakage). They are kept only because they are the
only ladders that show the closed roster at all.

Do not put these in a talk, paper, or handoff summary. The clean equivalents are the
`*_openonly.png` ladders in the parent directory.

Restore path: once `outputs/closed_reasoning/` finishes and `code/29_dual_human_ladders.py`
is re-run against v3 closed rows, drop the `STALE_` prefix and move back up one level.

See `../FIGURE_MANIFEST.md` for per-file rows.
